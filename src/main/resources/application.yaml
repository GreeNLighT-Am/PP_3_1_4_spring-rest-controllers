spring:
  application:
    name: springboot

  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_security_bootstrap?allowPublicKeyRetrieval=true&useSSL=false&verifyServerCertificate=false&useSSL=false&requireSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true